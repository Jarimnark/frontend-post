import { DatewithTimePipe } from './datewith-time.pipe';

describe('DatewithTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DatewithTimePipe();
    expect(pipe).toBeTruthy();
  });
});
